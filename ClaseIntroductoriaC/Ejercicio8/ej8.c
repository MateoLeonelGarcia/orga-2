#include <stdio.h>
#include <stdint.h>

int main(){
        
        uint8_t i = 5;

        printf("i vale 5, el valor que devuelve i++ es: %i\n", i++);
        printf("i vale 6, el valor que devuelve ++i es: %i \n",++i);
                        
        return 0;
}